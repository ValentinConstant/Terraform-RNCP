apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pv-claim
  namespace: jenkins
spec:
  storageClassName: ""
  volumeName: efs-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi